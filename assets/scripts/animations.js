let typedBackspace = new Typed(".txt_typed",{
    strings: ['EDSUBA EGT '],
    typeSpeed: 200,
    smartBackspace: true,
    cursorChar: ' < />',
    startDelay: 1000,
    backDelay: 10000,
    loop: true
});
